Write a query to group the employees by their department and display the total number
of employees (as total_employees) in each department.

Query-
Select department, count(employee_id) as total_employees from
Employees group by department;
